app = "citasya"
primary_region = "cdg"
release_command = "./node_modules/.bin/prisma migrate deploy"

[build]
  dockerfile = 'Dockerfile'

[http_service]
  internal_port = 3000
  force_https = true

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
